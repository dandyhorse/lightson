extends Node3D


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#Input.set_mouse_mode(Input.MOUSE_MasdawdsaODE_CONFINED)
	#var mouse_position = Vector2($Player.get_node("CameraPivot").position.x, $Player.get_node("CameraPivot").position.y)
	#print($Player.get_node("CameraPivot").position.x)
	#Input.warp_mouse(mouse_position)
	#get_viewport().warp_mouse(mouse_position)
	
	
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta: float) -> void:
	pass
